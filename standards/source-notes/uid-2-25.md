# UUID-Derived 2.25 UID Root

Checked: 2026-06-13  
Standards baseline: 2026b, `standards.lock.json`

## Affected Project Surface

- Case IDs: all generated cases
- Recipe IDs: all recipes that create Study, Series, SOP Instance, Frame of
  Reference, Implementation Class, or derived-reference UIDs
- Schema fields or validation rules: manifest `uids` fields and generated Part
  10 file meta/dataset UID consistency checks

## Required Decision

The generator needs deterministic DICOM UID values that are valid without
requesting an organization-specific root. The project uses UUID-derived
`2.25.<decimal uuid>` values and keeps the decimal UUID payload within the
DICOM UID length limit.

## KB Query

- Tool: `dicom_search_standard_text`
- Input: part filter `PS3.5`, query `2.25 UUID UID root`
- Edition returned: 2026b
- Source manifest SHA-256:
  `9959bee76fd293c7eda3fc81ce2ced7528612faa1b2df28cccd01504a83f54b0`
- Result: no matching standard text node
- Why insufficient: the current KB parser surface did not expose the PS3.5 UID
  text needed to cite the UUID-derived root directly.

## Official Source Evidence

- Part: PS3.5
- Section, table, or anchor: Section 9, Unique Identifiers
- Source artifact identity from `standards.lock.json`: PS3.5 PDF entry is
  present but has `status: not_downloaded` and `sha256: null`
- Concise evidence summary: DICOM UIDs are dot-separated numeric components
  with a 64-character maximum. The `2.25` root is used for UUID-derived UIDs
  represented as a decimal integer.

## Project Action

- Registry status: planned
- Registry reason or linked issue: none
- Should become KB patch: yes
- Expected cleanup after KB coverage exists: replace this source note with
  normal `dicom-standard-kb` evidence or add a direct KB evidence entry for the
  UID generator.
